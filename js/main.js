// For this exercise, you need to create a single JavaScript function named addExcitement that will use console.log() to print out a sentence to the browser console.

function addExcitement(excited) {
    console.log(excited +"!")
}

addExcitement("hello")